# Imagens da campanha

A referência pública usa apenas placeholders para as duas fotos do candidato. Substitua-os no HTML por imagens reais, preservando as proporções `1:1` (hero) e `4:3` (biografia).
